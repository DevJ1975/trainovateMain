node_modules/
.next/
out/
build/
dist/
.env*.local
.DS_Store
*.log
.vercel
next-env.d.ts.bak
tsconfig.tsbuildinfo

# Local SQLite (Phase 1 dev DB; production uses Postgres via DATABASE_URL)
near-miss.db
near-miss.db-journal
near-miss.db-wal
near-miss.db-shm

# Local attachment storage (Phase 1 dev; production uses S3 or similar)
uploads/
